@model BlogNullReference.DataServices.Services.Dto.TagDto[]

@if (Model != null || Model.Length > 0)
{
    <div class="tags">
        @foreach (var tag in Model)
        {
            <a
               href="@Url.Action("tag", "post", new { tag.Name })">
                @tag.Name
            </a>
        }
    </div>
}
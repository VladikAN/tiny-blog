@{
    var enableGoogleTags = ViewData[nameof(LayoutViewModel.GoogleTagsCode)] != null
        && ViewData[nameof(LayoutViewModel.GoogleTagsCode)] is string;
}

@if (enableGoogleTags)
{
    var code = ViewData[nameof(LayoutViewModel.GoogleTagsCode)] as string;

    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=@(code)"height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
}